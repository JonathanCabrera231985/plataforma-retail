{
  "name": "plataforma-retail-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:common": "npm run build --workspace=apps/common",
    "install:all": "npm install"
  }
}
